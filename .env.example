DATABASE_URL=postgresql://postgres@localhost:5432/mneme

# "ollama" (default) or "openai" — each auto-fills URLs, models, dimensions.
PROVIDER=ollama

# Required when PROVIDER=openai
PROVIDER_API_KEY=
